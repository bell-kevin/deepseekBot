{
  "name": "flash-chat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "A tiny anonymous DeepSeek V4 Flash 0731 chat app.",
  "scripts": {
    "source": "node scripts/create-source-bundle.mjs",
    "dev": "npm run source && node server.mjs --dev",
    "build": "vite build && node scripts/create-source-bundle.mjs",
    "preview": "vite preview",
    "serve": "node server.mjs",
    "start": "npm run build && npm run serve",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "vite": "8.2.0"
  }
}
